

-- create table pasteBin (
--     id serial primary key,
--     name text not null,
--     pasteTitle varchar(100),
--     pasteContent text not null
-- )

-- select * from pasteBin order by id desc limit 10;

-- create table commentsDb (
--   pasteId int, 
--   comment text,
  
--   FOREIGN KEY(pasteId) REFERENCES pasteBin(id)
--   )
  
--   insert into commentsDb (pasteId, comment)
--   values (12, 'i am your first comment'), (13, 'i am a rival commnet, boooo number 12')

-- select * from commentsDb

